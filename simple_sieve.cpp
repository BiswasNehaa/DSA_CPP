// simple sieve is an efficient algorithm to find all prime numbers up to a given limit n. It works by iteratively marking the multiples of each prime number starting from 2. The numbers that remain unmarked at the end of the process are the prime numbers.
// Time Complexity: O(n log log n)
// Space Complexity: O(n)

#include <iostream>
using namespace std;

void simple_sieve(int n)
{
    
}
int main()
{
    int n;
    cin>>n;
    simple_sieve(n);
    return 0;           
}
